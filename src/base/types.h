#pragma once

#include <string>
#include <vector>
#include <memory>
#include <cstdint>

namespace grabbed
{
    using u32 = uint32_t;
    using i32 = int32_t;
    using u16 = uint16_t;
    using i16 = int16_t;
    using u8 = uint8_t;
    using i8 = int8_t;
    using f32 = float;
    using string = std::string;
    using wstring = std::wstring;
    using size_t = std::size_t;
    using buffer = std::vector<u8>;
    using hash = uint32_t;
}
